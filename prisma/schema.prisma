generator client {
  provider = "prisma-client-js"
  
}

datasource db {
  provider = "postgresql"
}

enum MessageType {
  STATUS
}
model live_channel_message {
  message_location    String @id
  type          String @default("STATUS")
  last_fetched  Json   @default("{}")
}
